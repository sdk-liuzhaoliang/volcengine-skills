# commands/

存放仓库提供的 slash commands（`/command-name`）。

每个命令一个 `.md` 文件，文件名即命令名。YAML frontmatter 描述元信息，正文是命令展开后的 prompt。

参考 [Claude Code slash commands 文档](https://docs.claude.com/en/docs/claude-code/slash-commands)。
